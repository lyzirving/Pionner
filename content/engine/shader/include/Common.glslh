#pragma once

// SRGB to Linear Space
vec3 Mon2Lin(vec3 x) { return pow(x, vec3(2.2)); }

// Nearest int value
int NeInt(float val)
{
    float index = floor(val + 0.5f);
	return int(index);
}

float When_Eqi(int x, int y) { return 1.f - abs(float(sign(x - y))); }